// Authentication reducers for LOGIN/REGISTER
export default function(
  state = {
    hostName: 'https://fitter.best/api',
  },
  action,
) {
  switch (action.type) {
    default:
      return state;
  }
}
